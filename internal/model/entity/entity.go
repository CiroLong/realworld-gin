package entity

//  DB Entity（GORM Model）
//  这个包下主要是原始的gorm定义
//  entity的职责： 映射数据库

// 注意，这里的建表操作全部是由orm完成的，由于没有使用orm的关系机制，orm也没有生成外键

// AI：
// 在这个项目里我选择不在数据库层加外键约束，而是在应用层保证一致性，
// 主要考虑到高并发写入下外键带来的锁和迁移复杂度问题。
// Repo 层的写操作都是封装好的，不存在随意写脏数据的入口。
